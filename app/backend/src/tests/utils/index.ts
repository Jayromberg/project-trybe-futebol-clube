import mockMatches from "./mock.matches";
import mockLeaderboard from "./mock.leaderboard";
import mockLeaderboardAway from "./mock.leaderboard.away";
import mockLeaderboardHome from "./mock.leaderboard.home";

export {
  mockMatches,
  mockLeaderboard,
  mockLeaderboardAway,
  mockLeaderboardHome,
}